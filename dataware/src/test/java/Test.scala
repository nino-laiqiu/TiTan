import scala.collection.mutable.ListBuffer

object Test {
  def main(args: Array[String]): Unit = {
    var strings = ListBuffer[(String,Int)]()
     strings += (("s",1))
     val set = strings.toSet

  }
}
